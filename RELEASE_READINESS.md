# Sketch Arena release-readiness matrix

Audit date: 2026-08-18

This is the requirement-to-evidence record for the iSketch/SketchArena production-finish brief. **Proven locally** means current source plus direct automated or browser evidence supports the claim. **Live verified** means the named public service or Shido state was independently read on the stated date. Exact contract evidence is pinned in `PANIC_ARCHIVE_MAINNET_EVIDENCE.md`.

| Area | Status | Authoritative evidence | Remaining gate |
| --- | --- | --- | --- |
| Server-authoritative rooms, turns, prompts, timers and scores | Proven locally | `GameRoom.test.ts` covers lifecycle, scoring, hints, disconnect pause/resume and edge cases; prompt data is filtered per player in `GameRoom.view()` | None for single-instance launch |
| Real transport lifecycle | Proven locally | `socket.integration.test.ts` launches the compiled server and real Socket.IO clients, then verifies join, duplicate-tab replacement, stale-action rejection, stale-disconnect safety and host migration | Multi-region/horizontal scaling requires Redis-backed room state |
| Drawing and guessing | Proven locally | Zod-bounded strokes, shared client/server point limits, seamless long-gesture segmentation, acknowledged permanent saves, independent preview budgets, server-owned drawer checks, answer-leak prevention, spelling tolerance, guess placement and score tests; a browser-held 620-point line survived delayed authoritative synchronization; the latest disposable loopback run sustained 32 clients in 4 rooms and 256 acknowledged actions with zero failures (21.3 ms p95) | Repeat the same commit against a production-sized staging host; the local single-process result is not an internet or multi-node capacity claim |
| Lobby and invitation flow | Proven locally | Public/private creation, invite codes, ready state, configurable deck/player-cap/timer rules, fixed cap-length matches, full/invalid room handling and browser walkthrough; host removal room-bans the removed session | AFK automation and spectator mode remain future work |
| Match length and drawer fairness | Proven locally | A room always produces the number of drawings promised by its player cap; two-player and uneven crews differ by at most one turn, and unfinished turns rebalance after a permanent departure | Final live browser walkthrough after deployment |
| Countdown, social energy and audio | Proven locally | Staged pressure states, tick/start/turn/reveal/victory hooks, reactions, funny/close guesses, persistent mute and accessible volume mixer; desktop and phone browser inspection | Replace synthesized hooks with final licensed sound assets if desired |
| Round reveal and match end | Proven locally | Reveal data includes drawer, solvers, fastest guess, gains, funny guesses and reactions; actual two-player match reached the full afterparty | None |
| Match gallery and trophy handoff | Proven locally | Every match drawing enlarges/downloads/reacts; only its drawer can idempotently save it; browser QA verified the saved trophy in the authenticated Vault | Public sharing of unminted private trophies is intentionally not claimed |
| Solo Studio | Proven locally | Professional brush library, color picker plus 8 essentials/24 saved colors, shapes, fill, sample, move, grid, symmetry, format library, layers, blend/opacity/lock/rename/duplicate/delete, SVG/Vault export and dark UI | Very large documents may eventually benefit from worker/off-main-thread rendering |
| Studio performance | Proven locally | Immutable layer render cache and reusable active-stroke surface remove per-pointer repaint/allocation of every layer; temporary stroke/undo browser QA preserved the draft; production dependencies are split into cacheable app, motion, realtime and passkey chunks with no chunk above 318 kB minified | Device profiling on final minimum hardware |
| Phone/tablet/desktop presentation | Proven locally | Canvas-first mobile arena drawers and Studio format/brush/layer dock; 390×844 entry, lobby, Archive, Arena mixer and Studio panels visually inspected with no horizontal overflow | Final physical iOS/Android safe-area and keyboard matrix |
| Accessibility | Proven locally | Semantic controls/dialogs, labels, focus-visible states, modal focus trap, Escape dismissal, reduced-motion handling, non-color labels and keyboard-first guessing; browser focus checks passed | Formal external screen-reader audit remains advisable |
| Vault ownership and recovery | Proven locally | Legacy 256-bit identities migrate without changing their durable UUID; only hashes are stored; HttpOnly SameSite device sessions expire and revoke independently; passkey registration/authentication uses exact RP/origin verification, required user verification, one-time challenges and persisted counters; recovery restore signs the current device out first | Run the physical Face ID/Touch ID/Windows Hello/Android matrix and publish account-recovery support policy |
| PostgreSQL persistence | Live verified | Production uses one shared protected PostgreSQL pool for accounts/passkeys, Vault artwork, wallet/voucher/mints, progression/rewards, promotions, moderation and leaderboards; checksum migrations pass in CI and production; the 2026-08-18 backup restored all 10 migrations and representative counts from every durable domain into an isolated database | Configure off-host replication/retention and prove point-in-time recovery |
| Season 0, Battle Pass, achievements and items | Live beta behavior verified | Durable PostgreSQL progression, idempotent match awards/campaigns, reward acknowledgements, Mint Credits, discounts, achievements and hidden future entitlements; Season 0 exposes only its useful free beta track and rejects premium grants at the API; owned catalog cosmetics validate/equip by slot; avatars affect identity and brush items change the real Studio renderer | Season 1 Premium commerce remains a separate future gate requiring an approved USD provider, price, signed idempotent orders/webhooks and refund policy |
| Promotions and giveaways | Proven locally | High-entropy hashed capped/expiring/pausable promo codes, one redemption per player, individual grants and preview-confirmed all-player drops with role/audit boundaries | Staff operating policy and multisig approval process |
| API/socket abuse resistance | Proven locally | Input schemas, body/stroke/point caps, per-IP/API/wallet/promo limits, per-socket action/guess/draw budgets, active-socket binding, CORS/Helmet and proxy-safe IP resolution | Independent penetration test and production WAF tuning |
| Production supervision | Proven locally | `/health/live`, `/health/ready`, starting/ready/draining lifecycle tests, correlated JSON logs, fatal handling, graceful shutdown, release SHA evidence and Nginx/systemd examples; bearer-protected localhost Prometheus metrics cover load, failures, limits and memory | Install the host collector, alert rules and external error aggregation |
| Backup integrity | Live restore verified | The least-privilege production timer creates custom-format dumps with restrictive SHA-256 manifests and validates them with `pg_restore --list`; backup `2026-08-18T19:42:20.710Z` was restored into a uniquely named temporary database, checked across all durable domains and removed; the app role correctly lacks database-creation privilege | Configure off-host replication/retention and prove point-in-time recovery |
| Backstage production access | Proven locally | Named hashed viewer/operator/admin principals, duplicate detection and production fail-closed startup; shared compatibility token requires an explicit migration override | Provision real named staff credentials, document rotation and add upstream MFA/SSO if required |
| Player moderation | Proven locally | Host removal room-bans the target and rebalances active turns; any player can file a private categorized report against an authoritative room identity; duplicate/hour limits, protected durable storage, staff-only evidence and named operator resolution are covered through Socket.IO-to-Backstage integration | Publish community rules, staff response targets, escalation/appeal policy and rehearse the live moderation runbook |
| Wallet ownership proof | Proven locally and exercised live | Five-minute single-use challenges, real signature verification, durable binding and matching-wallet reuse; secrets remain server-only; a production wallet completed the canary mint | Complete the final physical desktop/mobile wallet matrix |
| Voucher pricing and rewards | Proven locally | Server resolves Mint Credit/discount eligibility, reserves one entitlement unit and signs exact recipient/URI/artwork/price/nonce/deadline/season/campaign fields | Secure production signer provisioning and rotation ceremony |
| IPFS preparation | Proven locally | Server renders canonical SVG, pins media then public-safe metadata, rejects invalid/storage failures and never exposes the private Kubo API | Verify NFTStudio production Kubo credentials, retention and backup policy |
| Mint confirmation | Live verified | Server independently verifies transaction sender/contract, receipt status, confirmation depth and exact `PanicArchiveMinted` fields; the paid PANIC #1 mainnet canary receipt and event were independently re-read | Add recurring RPC/wallet chaos drills |
| Wallet failure UX | Proven locally | Classified cancellation, authorization, disconnection, wrong chain, pending request, insufficient gas, replacement, nonce, revert, RPC and safe fallback messages; polling cancels on close and submissions are immediately locked | Live wallet/RPC chaos matrix on testnet |
| Panic Archive contract | Live verified; independent audit outstanding | Solidity 0.8.26 runtime is 14,381 bytes and matches the checked-in artifact; live roles, policy, paid PANIC #1 receipt, metadata and retained original-artist royalty were independently checked | Independent audit, any auditor-requested extended fuzzing and migration of ownership to a reviewed multisig when available |
| Backstage contract controls | Implemented and live-address aware | Block, approve and allowlist transactions decode against the exact Solidity selectors; owner wallet must review and submit them | Rehearse emergency pause and signer rotation without exposing keys |
| Public Panic Archive | Live verified | Anonymous confirmed-only feed exposes PANIC #1 while excluding drafts, pending mints, wallet bindings and private session IDs | Continue privacy/regression monitoring as supply grows |
| Marketplace handoff | Live verified | NFTStudio Marketplace V2 reports the collection approved; the public indexer exposes The Panic Archive and PANIC #1 under the canonical on-chain slug | Monitor indexing delay/refresh behavior and prove purchase/cancel/settlement receipts |
| Release gates | Live verified | `npm run check` passes strict lint, all TypeScript/production builds, 80 always-on server tests, 9 web tests and 10 operations tests; all six PostgreSQL adapters raise the CI server total to 86; `npm run qa:load`, pinned contract compilation/adversarial evidence and zero high production advisories pass; GitHub gates and the live SHA match `457a94d42f000149ad408ec4efb58d13e68de904` | Continue running the same gates for every release |

## Current release decision

The application, minting boundary, deployed Panic Archive and NFTStudio collection handoff are live beta quality. The irreversible on-chain state is recorded in `PANIC_ARCHIVE_MAINNET_EVIDENCE.md`. The beta label should remain until an independent Solidity/security review, physical mobile/passkey/wallet matrix, restore drill and ongoing production monitoring are complete.

### Public-host evidence

On 2026-08-18, `https://sketch.bodhix.io/` served the isolated recovery-tested release at Git commit `457a94d42f000149ad408ec4efb58d13e68de904`; both health endpoints were healthy and mint readiness was enabled. The live client exposes only the free Season 0 beta track and its admin bundle contains no premium-pass grant option. PANIC #1, public IPFS media/metadata, collection approval and NFTStudio indexing were independently verified. The latest verified PostgreSQL backup was restored, checked and removed through the least-privilege drill without altering the production database.

## External handoff sequence

1. Commission an independent Solidity/security review and run any requested extended fuzz/property campaigns.
2. Rehearse emergency pause, signer rotation, owner recovery and marketplace cancellation/settlement with receipt evidence.
3. Complete physical Android/iOS/desktop wallet, passkey, keyboard, safe-area and Studio gesture testing.
4. Execute and document an actual PostgreSQL restore/PITR drill plus off-host retention.
5. Keep external health, error-rate, signer, RPC, mint and indexer alerts active before widening beta access.
